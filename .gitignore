# 1. 의존성 패키지 격리 버스 (Node.js & Python Dependencies)
node_modules/
.venv/
.env
.env.local
.env.*.local

# 2. 컴파일 아티팩트 및 캐시 툴체인 (Build Outputs & Cache Guards)
dist/
dist-ssr/
build/
wheels/
*.egg-info/
__pycache__/
*.py[cod]
*$py.class

# uv 패키지 매니저 및 Vite 번들러 내부 가상 런타임 캐시 완전 차단
.uv/
.vite/
.eslintcache
.parcel-cache
.turbo/

# 3. 로컬 퍼스트 AI 자산 및 임베딩 모델 가드레일 (Heavy ONNX & Vector Assets)
model/
bge-m3-onnx/
bge-m3-onnx-int8/

# 4. 데이터베이스 및 물리 벡터 인덱스 경로 구체화 (DB & FAISS Index Security)
# 소스 코드 오탐을 방지하기 위해 db 폴더 하부의 영속 바이너리만 타격 차단
db/*.db
db/*.index
*.db-journal
db_backup/

# 5. 북마크 백업 JSON 가드 및 환경 정의서 구출
*bookmark*.json
bookmark-*.json

# 6. IDE 및 운영체제 노이즈 자산 (System & Editor Metadata)
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 7. 로그
log/
*.log

# 8. 사용자 지정 파일
test/
spec/

# 9. 예외 보존 가이드 문서 규칙 (Exceptions)
!README.md
!.env.example
